Ínicio:
npm init -y
npm i -g typescript
npm i ts-node
npm i express mysql2 dotenv @types/express @types/node
tsc --init
Para rodar o servidor, utiliar o comando "npx ts-node src/app.ts"
 (Re utilizar o npm i sempre que repassar os arquivos do Git para outras máquinas e refazer o ".env")
export interface Produto{
    id?: number; (A interrogação significa opcionalidade)
    nome: string;
    preco: number
}

MVC 

Model CRIAR LISTAR ALTERAR DELETAR

Controller

Rotas 

Servidor 

Front CRIAR LISTAR ALTERAR DELETAR

Para rodar o servidor, utiliar o comando "npx ts-node src/app.ts"

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=aluno
DB_DATABASE=crud_ts
DB_PORT=3302 

__________________________________________________________-

npm init -y 
npm i -g typescript
npm i ts-node

npm i express mysql2 dotenv @types/express @types/node
 
baixar express, banco de datos, arquivos env

tsc --init
"?" serve para indicar que a propriedade ligada ao objeto é opcional.
--


MVC

Model                criar listar alterara e deletar

Controler 

ROTAS
SERVIDOR
FRONT    CRISR LISTAR ALTERAR DELETAR


_______________________________________________

select * from produtos

buscar no banco d dados


______________________________________________________
.env

DB_HOST:localhost
DB_USER:root
DB_PASSWORD:aluno
DB_DATABASE:crud_ts
DB_PORT:3302

____________










--------------

create database crud_ts;
use crud_ts;

create table produtos(
id int auto_increment primary key,
nome varchar(258) not null,
preco decimal(10,2) not null
);

_______________________________________________




para rodar o server usar o comando 

npx ts-node src/app.ts



________________________